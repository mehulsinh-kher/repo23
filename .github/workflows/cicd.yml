name: cicd

on:
  push:
    branches: [master]
  workflow_dispatch:


jobs:

  code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        run: echo "code clone from github"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker Container
        run: echo "Docker Container Builded"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Testing Code
        run: echo "Code Tested"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying App
        run: echo "Docker Deployed The APP"
